import { defineMessages } from 'react-intl';

export default defineMessages({
  invalidOrderPredicate: {
    id: 'CompanyOrders.Filters.invalidOrderPredicate',
    description: 'Error message shown when the order predicate is invalid',
    defaultMessage:
      'The order predicate given in the filters is invalid. Please verify its syntax.',
  },
});
